name: Build OpenWrt (First Boot Install)

on:
  workflow_dispatch:
    inputs:
      router_ip:
        description: '請設置路由器的管理地址'
        required: true
        default: '192.168.10.1'
        type: string
      firmware_size:
        description: '請輸入韌體大小 (MB)'
        required: true
        default: '2048'
        type: string
      include_openclash:
        description: '是否安裝 OpenClash？'
        required: true
        default: 'yes'
        type: choice
        options:
          - 'yes'
          - 'no'
      include_docker:
        description: '是否安裝 Docker？'
        required: true
        default: 'yes'
        type: choice
        options:
          - 'yes'
          - 'no'
      include_ext_apps:
        description: '是否安裝常用外掛？'
        required: true
        default: 'yes'
        type: choice
        options:
          - 'yes'
          - 'no'

permissions:
  contents: write

jobs:
  build:
    name: Build Firmware
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Initialize Environment
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libncurses5-dev libncursesw5-dev \
        zlib1g-dev gawk git gettext libssl-dev xsltproc rsync wget unzip python3 qemu-utils
        sudo apt-get clean

    - name: Download Image Builder
      run: |
        echo "正在偵測官方伺服器上的最新穩定版..."
        BASE_URL="https://downloads.openwrt.org/releases"
        VERSION=$(curl -s "$BASE_URL/" | grep -oE '>[0-9]+\.[0-9]+\.[0-9]+' | cut -d '>' -f 2 | sort -V | tail -n 1)
        
        echo "偵測到最新可用版本: $VERSION"
        TARGET_URL="$BASE_URL/$VERSION/targets/x86/64"
        
        wget -O sha256sums "$TARGET_URL/sha256sums"
        IB_FILENAME=$(grep "openwrt-imagebuilder" sha256sums | grep "x86-64" | head -n 1 | awk '{print $2}' | tr -d '*')
        
        if [ -z "$IB_FILENAME" ]; then
          echo "錯誤：無法找到 Image Builder 檔案！"
          exit 1
        fi
        
        echo "正在下載: $IB_FILENAME"
        wget -O imagebuilder.tar.xz "$TARGET_URL/$IB_FILENAME"
        mkdir -p openwrt
        tar -xf imagebuilder.tar.xz --strip-components=1 -C openwrt
        
        echo "OPENWRT_VERSION=$VERSION" >> $GITHUB_ENV
        echo "BUILD_DATE=$(date +"%Y%m%d")" >> $GITHUB_ENV

    - name: Configure Firmware
      working-directory: ./openwrt
      run: |
        # 1. 設定 2GB 空間
        sed -i '/CONFIG_TARGET_ROOTFS_PARTSIZE/d' .config
        echo "CONFIG_TARGET_ROOTFS_PARTSIZE=${{ github.event.inputs.firmware_size }}" >> .config
        
        # 2. 建立自定義檔案結構
        mkdir -p files/etc/uci-defaults
        mkdir -p files/root
        
        # 3. 設定 IP 與中文
        cat << "EOF" > files/etc/uci-defaults/99-custom-settings
        #!/bin/sh
        uci set network.lan.ipaddr='${{ github.event.inputs.router_ip }}'
        # 嘗試設定中文 (如果有的話)
        uci set luci.main.lang='zh_tw'
        uci commit
        exit 0
        EOF
        chmod +x files/etc/uci-defaults/99-custom-settings

    - name: Prepare OpenClash (Offline Install Mode)
      working-directory: ./openwrt
      run: |
        if [ "${{ github.event.inputs.include_openclash }}" = "yes" ]; then
          echo "正在下載 OpenClash APK..."
          
          # 嘗試抓取最新版
          CLASH_URL=$(curl -sL https://api.github.com/repos/vernesong/OpenClash/releases/latest | grep -oE 'https://[^"]*luci-app-openclash[^"]*\.apk' | head -n 1)
          
          # 保底機制
          if [ -z "$CLASH_URL" ]; then
            CLASH_URL="https://github.com/vernesong/OpenClash/releases/download/v0.47.055/luci-app-openclash-0.47.055.apk"
          fi
          
          echo "下載目標: $CLASH_URL"
          # 直接下載到 files/root/ 目錄，這樣會被打包進韌體的 /root/ 底下
          wget -O files/root/luci-app-openclash.apk "$CLASH_URL"
          
          # 建立開機自動安裝腳本
          cat << "EOF" > files/etc/uci-defaults/99-install-openclash
          #!/bin/sh
          # 等待系統就緒
          sleep 5
          echo "正在安裝 OpenClash..."
          # 強制安裝我們預載的 apk
          apk add --allow-untrusted --force-overwrite /root/luci-app-openclash.apk
          # 安裝完後刪除安裝檔以節省空間
          rm -f /root/luci-app-openclash.apk
          exit 0
          EOF
          chmod +x files/etc/uci-defaults/99-install-openclash
          
          echo "OpenClash 已準備就緒 (將於開機時安裝)"
        fi

    - name: Build Firmware
      working-directory: ./openwrt
      run: |
        # 1. 基礎系統與工具
        PACKAGES="base-files libc ca-bundle curl wget bash unzip gzip \
        luci luci-ssl luci-base luci-compat \
        luci-i18n-base-zh-tw \
        luci-app-attendedsysupgrade \
        parted losetup resize2fs lsblk"
        
        # 2. OpenClash 的所有依賴 (我們先裝好這些，開機裝 apk 就不會報錯)
        if [ "${{ github.event.inputs.include_openclash }}" = "yes" ]; then
            PACKAGES="$PACKAGES -dnsmasq dnsmasq-full \
            ruby ruby-yaml kmod-tun kmod-inet-diag kmod-nft-tproxy libcap libcap-bin"
        fi
        
        # 3. Docker
        if [ "${{ github.event.inputs.include_docker }}" = "yes" ]; then
            PACKAGES="$PACKAGES docker dockerd luci-app-dockerman"
        fi
        
        # 4. 常用外掛
        if [ "${{ github.event.inputs.include_ext_apps }}" = "yes" ]; then
            PACKAGES="$PACKAGES luci-app-upnp luci-app-samba4 luci-app-ttyd luci-app-wol luci-app-ddns"
        fi
        
        echo "開始打包..."
        # 注意：這裡不加 luci-app-openclash 到 PACKAGES，因為我們會開機後手動裝
        make image PROFILE=generic PACKAGES="$PACKAGES" FILES="files" EXTRA_kmod_fs_ext4=y BIN_DIR="bin/targets/x86/64"

    - name: Process Files
      working-directory: ./openwrt/bin/targets/x86/64
      run: |
        echo "正在處理檔案..."
        mkdir -p ../../../../release_files
        
        for file in *combined-efi.img.gz; do
          if [ -f "$file" ]; then
            cp "$file" "../../../../release_files/"
            
            # 解壓並轉 VMDK
            gunzip -k "$file"
            raw_file=${file%.gz}
            vmdk_file=${raw_file%.img}.vmdk
            
            echo "轉換 VMDK: $raw_file -> $vmdk_file"
            qemu-img convert -f raw -O vmdk "$raw_file" "../../../../release_files/$vmdk_file"
          fi
        done

    - name: Compress VMDK
      run: |
        cd release_files
        gzip *.vmdk

    - name: Upload to Releases
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ env.OPENWRT_VERSION }}-${{ env.BUILD_DATE }}
        name: OpenWrt v${{ env.OPENWRT_VERSION }} (Auto Install)
        files: release_files/*
        body: |
          ### OpenWrt x86_64 穩定版 (Image Builder)
          
          * **核心版本**: v${{ env.OPENWRT_VERSION }}
          * **打包日期**: ${{ env.BUILD_DATE }}
          
          #### 🔐 資訊：
          * **IP**: `${{ github.event.inputs.router_ip }}`
          * **帳號**: `root` / **密碼**: `無`
          
          #### 🚀 功能：
          * **OpenClash**: ${{ github.event.inputs.include_openclash }} (開機後自動安裝)
          * **Docker**: ${{ github.event.inputs.include_docker }}
          
          #### 💾 空間：
          * **系統分區**: `${{ github.event.inputs.firmware_size }} MB`
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
